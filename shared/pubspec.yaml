name: shared
description: Shared models for workout app and backend
version: 1.0.0
# publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  lints: ^2.1.1
  test: ^1.24.6
